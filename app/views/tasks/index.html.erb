<%= link_to "投稿画面へ", new_task_path %>
<% @tasks.each do |task| %>
  <p>
    <!-- 以下の2行のコードを追加 -->
    <%= task.title %>
    <%= link_to "詳細", task_path(task.id) %>
    <%= link_to "編集", edit_task_path(task.id) %>
  </p>
<% end %>